import 'package:flutter/material.dart';
import 'package:get/get.dart';

import 'rs_controller.dart';

class RsPage extends StatelessWidget {
  final controller = Get.put(RsController());

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
