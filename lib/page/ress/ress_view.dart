import 'package:flutter/material.dart';
import 'package:get/get.dart';

import 'ress_controller.dart';

class RessPage extends StatelessWidget {

  final controller = Get.put(RessController());

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
